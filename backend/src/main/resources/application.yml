
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/authserver}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:1234}
    driver-class-name: org.postgresql.Driver

  jpa:
    # Disabled because long-lived SSE requests (/api/contest/stream,
    # /api/team/stream) would otherwise hold a Hikari connection for the
    # entire 30-minute stream lifetime, exhausting the pool and timing out
    # /auth/refresh and the cold-load REST probe. DTO mapping already
    # happens inside @Transactional service methods, so disabling OSIV
    # is safe here.
    open-in-view: false
    hibernate:
      ddl-auto: ${SPRING_JPA_DDL_AUTO:validate}
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: ${SPRING_FLYWAY_ENABLED:true}
    locations: classpath:db/migration
    baseline-on-migrate: ${SPRING_FLYWAY_BASELINE_ON_MIGRATE:false}
    baseline-version: ${SPRING_FLYWAY_BASELINE_VERSION:0}

  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}

  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:namelessorgao@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:caku ucxf cvuq cicb}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

judge0:
  url: ${JUDGE0_URL:https://ce.judge0.com/submissions?wait=false}
  callback: ${JUDGE0_CALLBACK_URL:http://localhost:8080/api/callback/judge0}
  # Dev fallback keeps local runs simple. Override in production with JUDGE0_CALLBACK_SECRET.
  callback-secret: ${JUDGE0_CALLBACK_SECRET:dev-only-change-me}
  validator:
    cpu-time-limit-seconds: ${JUDGE0_VALIDATOR_CPU_TIME_LIMIT_SECONDS:2.0}
    memory-limit-kilobytes: ${JUDGE0_VALIDATOR_MEMORY_LIMIT_KILOBYTES:131072}

contest:
  sync:
    enabled: ${CONTEST_SYNC_ENABLED:true}
    delay-ms: ${CONTEST_SYNC_DELAY_MS:30000}
    initial-delay-ms: ${CONTEST_SYNC_INITIAL_DELAY_MS:10000}

jwt:
  #dont change me. hallaq cry to make it work :) (i have 64B problem)
  # Dev-safe fallbacks let IDE runs work even when env vars are not configured.
  access-secret: ${JWT_ACCESS_SECRET:HrsxedVrtFb4LRvJXWmNbN/UAWhz6i0yZQH4DafTEZo=}
  refresh-secret: ${JWT_REFRESH_SECRET:TAvuy/amtiO5tEpBHUVArpY8pG6syxwTAqfYYZxRHnI=}
  expiration: 900000        # 15 minutes
  refresh-expiration: 604800000 # 7 days

bootstrap:
  admin:
    username: ${BOOTSTRAP_ADMIN_USERNAME:admin}
    credentials-file: ${BOOTSTRAP_ADMIN_CREDENTIALS_FILE:admin-account.txt}
    reset-existing-password: ${BOOTSTRAP_ADMIN_RESET_EXISTING_PASSWORD:false}

springdoc:
  swagger-ui:
    enabled: true
